// app/(auth)/login/page.tsx — Login page: name picker list → email modal → Firebase auth.
// Lists all non-disabled users from /users. Click name → modal → signIn(email).
// See CLAUDE.md section 8.2 for auth model.
// TODO: implement in Session 1

export default function LoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p className="text-muted-foreground">Login — Session 1</p>
    </div>
  );
}
