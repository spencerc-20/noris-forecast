// app/(app)/dashboard/page.tsx — Rep's own pipeline view with ForecastHeader + DealList.
// TODO: implement in Session 2

export default function DashboardPage() {
  return (
    <div className="p-8">
      <p className="text-muted-foreground">Rep Dashboard — Session 2</p>
    </div>
  );
}
