// app/(app)/admin/page.tsx — Admin panel: users, config, state-region map, CSV import, snapshots.
// Spencer-only. See CLAUDE.md section 8.11.
// TODO: implement in Session 10

export default function AdminPage() {
  return (
    <div className="p-8">
      <p className="text-muted-foreground">Admin — Session 10</p>
    </div>
  );
}
